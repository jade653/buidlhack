# TEE Engine — Python dependencies
#
# Install with:
#   pip install -r requirements.txt

# Sandbox
RestrictedPython>=7.0,<8.0

# LLM client (OpenAI-compatible, used for Near AI Cloud)
openai>=1.0,<2.0

# Optional: AI frameworks the user's harness may import.
# Install these for local testing of LangGraph / CrewAI harnesses.
# langgraph>=0.1
# crewai>=0.1
# langchain>=0.2
